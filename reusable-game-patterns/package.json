{
  "name": "reusable-game-patterns",
  "version": "1.0.0",
  "description": "Reusable game patterns for educational games",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "build:all": "npm run build",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "education",
    "games",
    "patterns",
    "reusable"
  ],
  "author": "AI Edu Chatbot",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^21.0.1",
    "@rollup/plugin-node-resolve": "^13.1.3",
    "@rollup/plugin-typescript": "^8.3.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.66.1",
    "typescript": "^4.5.5"
  }
}